# NO commitear este archivo con datos reales
# Crear con kubectl directamente:
#
#   kubectl create secret docker-registry docker-credentials \
#     --docker-server=https://index.docker.io/v1/ \
#     --docker-username=TU_USUARIO_DOCKERHUB \
#     --docker-password=TU_TOKEN_DOCKERHUB \
#     -n tekton-pipelines
#
# Este archivo es solo referencia de la estructura del Secret
apiVersion: v1
kind: Secret
metadata:
  name: docker-credentials
  namespace: tekton-pipelines
type: kubernetes.io/dockerconfigjson
data:
  .dockerconfigjson: <BASE64_DE_TU_CONFIG>   # NO USAR — crear con kubectl create secret
