apiVersion: v1
kind: Service
metadata:
  name: page-demo
  namespace: demo
  labels:
    app: page-demo
spec:
  type: NodePort
  selector:
    app: page-demo
  ports:
    - port: 80
      targetPort: 80
      nodePort: 31080
